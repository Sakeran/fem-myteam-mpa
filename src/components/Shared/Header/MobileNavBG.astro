<svg
    viewBox="-1 0 100 200"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    aria-hidden="true"
>
    <path
        fill-rule="evenodd"
        clip-rule="evenodd"
        d="M100 1C100.334 1 100.667 1.00163 101 1.0049V67C82.7746 67 68 81.7746 68 100C68 118.225 82.7746 133 101 133V200.995C100.667 200.998 100.334 201 100 201C44.7715 201 0 156.228 0 101C0 45.7715 44.7715 1 100 1Z"
        fill="#F67E7E"></path>
    <rect x="0.997803" width="100" height="4" fill="#79C8C7"></rect>
    <rect x="0.997803" y="8" width="100" height="4" fill="#79C8C7"></rect>
    <rect x="0.997803" y="16" width="100" height="4" fill="#79C8C7"></rect>
    <rect x="0.997803" y="24" width="100" height="4" fill="#79C8C7"></rect>
    <rect x="0.997803" y="32" width="100" height="4" fill="#79C8C7"></rect>
    <rect x="0.997803" y="40" width="100" height="4" fill="#79C8C7"></rect>
</svg>

<style lang="postcss">
    svg {
        --popup-offset: 0.025s;
        --popup-delay: 0.25s;
        --popup-duration: 0.025s;

        --rotate-duration: 0.1s;
        --rotate-delay: calc(
            -0.125s + var(--popup-delay) + var(--popup-duration) + (6 *
                        var(--popup-offset))
        );
    }

    svg path {
        transform-origin: center right;
        animation-timing-function: linear;
        animation-timing-function: ease-in-out;
        animation-duration: var(--rotate-duration);
        animation-fill-mode: both;
        animation-delay: var(--rotate-delay);
    }

    svg rect {
        animation-timing-function: ease-in-out;
        animation-duration: var(--popup-duration);
        animation-fill-mode: both;
    }

    svg rect:nth-of-type(1) {
        animation-delay: calc(var(--popup-delay) + (5 * var(--popup-offset)));
    }

    svg rect:nth-of-type(2) {
        animation-delay: calc(var(--popup-delay) + (4 * var(--popup-offset)));
    }

    svg rect:nth-of-type(3) {
        animation-delay: calc(var(--popup-delay) + (3 * var(--popup-offset)));
    }

    svg rect:nth-of-type(4) {
        animation-delay: calc(var(--popup-delay) + (2 * var(--popup-offset)));
    }

    svg rect:nth-of-type(5) {
        animation-delay: calc(var(--popup-delay) + (1 * var(--popup-offset)));
    }

    svg rect:nth-of-type(6) {
        animation-delay: calc(var(--popup-delay) + (0 * var(--popup-offset)));
    }

    @media (prefers-reduced-motion: no-preference) and (max-width: theme("screens.md")) {
        :global([data-nav-visible="true"]) svg path {
            animation-name: nav-bg-rotate-in;
        }

        :global([data-nav-visible="true"]) svg rect {
            animation-name: nav-bg-fadein;
        }
    }

    @keyframes nav-bg-rotate-in {
        from {
            transform: rotateZ(-180deg);
        }
        to {
            transform: rotateZ(0deg);
        }
    }

    @keyframes nav-bg-fadein {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }
</style>
